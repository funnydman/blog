sudo: required

language: python
python:
  - "3.6"

services:
  - docker
  
before_install:
  - sudo apt-get update
  - sudo apt-get -y -o Dpkg::Options::="--force-confnew" install docker-ce
  - docker-compose --version
  - docker-compose up
